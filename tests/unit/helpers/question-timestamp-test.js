
import { questionTimestamp } from 'js-message-board/helpers/question-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | question timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionTimestamp([42]);
  assert.ok(result);
});

