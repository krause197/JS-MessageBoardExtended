
import { answerTimestamp } from 'js-message-board/helpers/answer-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | answer timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerTimestamp([42]);
  assert.ok(result);
});

